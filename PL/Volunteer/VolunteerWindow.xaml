<Window x:Class="PL.Volunteer.VolunteerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Volunteer"
        mc:Ignorable="d"
        Title="VolunteerWindow" Height="450" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}" >
    <Window.Resources>
        <Style x:Key="StandardLabelStyle" TargetType="Label">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Width" Value="110"/>
            <!--<Setter Property="Margin" Value="5"/>-->
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="250*"/>
            <RowDefinition Height="100*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100*"/>
            <ColumnDefinition Width="100*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="0" Margin="50,15" Orientation="Vertical" HorizontalAlignment="Left">
          
            <!--id-->
            <StackPanel Orientation="Horizontal">
                <Label Content="id:"  Style="{StaticResource StandardLabelStyle}"/>
                <TextBox Text="325698" Width="100"  ></TextBox>
            </StackPanel>
          
            <!--name-string-->
            <StackPanel Orientation="Horizontal">
                <Label Content="name:"  Style="{StaticResource StandardLabelStyle}"/>
                <TextBox Text="yosi" Width="100"  ></TextBox>
            </StackPanel>
         
            <!--phone-srting-->
            <StackPanel Orientation="Horizontal">
                <Label Content="phone number:"  Style="{StaticResource StandardLabelStyle}"/>
                <TextBox Text="0583235695"  Width="100"  ></TextBox>
            </StackPanel>
            <!--email-srting-->
            <StackPanel Orientation="Horizontal">
                <Label Content="email:"  Style="{StaticResource StandardLabelStyle}"/>
                <TextBox Text="t@gmail" Width="100"  ></TextBox>
            </StackPanel>
            <!--position-enum-->
            <!--password-srting-->
            <StackPanel Orientation="Horizontal">
                <Label Content="name:"  Style="{StaticResource StandardLabelStyle}"/>
                <TextBox Text="yosi" Width="100"  ></TextBox>
            </StackPanel>
            <!--addres-srting-->
            <StackPanel Orientation="Horizontal">
                <Label Content="password:"  Style="{StaticResource StandardLabelStyle}"/>
                <TextBox Text="yosi" Width="100"  ></TextBox>
            </StackPanel>
            <!--maxium distance-double-->
            <StackPanel Orientation="Horizontal">
                <Label Content="maxium distance:"  Style="{StaticResource StandardLabelStyle}"/>
                <TextBox Text="60" Width="100"  ></TextBox>
            </StackPanel>
            <!--type of distance-enum-->
            
            <!--active-bool-->
            <StackPanel Orientation="Horizontal" >
                <Label Content="is active:"  Style="{StaticResource StandardLabelStyle}"/>
                <CheckBox Width="100" />
            </StackPanel>
        </StackPanel>

        <!--another-->
        <!--<StackPanel Grid.Row="0" Grid.Column="1" Margin="50,15" Orientation="Vertical" HorizontalAlignment="Left">
            <Button Content="Initialize" Click="btnIntialize_Click" Style="{StaticResource StandardButtonStyle}"></Button>
            <Button Content="ResetDB" Click="btnResetDB_Click" Style="{StaticResource StandardButtonStyle}"></Button>
        </StackPanel>-->
    </Grid>
</Window>
